// types/todo/category.ts

export interface Category {
  name: string;               // Category name (e.g., Work, Personal)
  color?: string;             // Optional color for visual indication
  style?: string;             // Optional styling class
}